name: Playwright CI with Docker

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest   # Docker works best here

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests in Docker
        run: |
          docker build -t playwright-ci .
          docker run playwright-ci